import Appointment from '../../layouts/client/Appointment';

const appointmentRoutes = [
    {
        path: 'appointment',
        element: <Appointment />,
    },
];

export default appointmentRoutes;
